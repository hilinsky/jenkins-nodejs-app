{
  "name": "multistage_nodejs_app",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1"
  },
  "scripts": {
  "build": "mkdir -p dist/views/css && cp views/index.html dist/views  && cp views/sharks.html dist/views && cp views/css/styles.css dist/views/css && cp app.js dist/"
}
}
